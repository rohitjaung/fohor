<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.example.fohormalai">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity android:name=".MainActivity_Start">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity_Second" />
        <activity android:name=".Registration" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity_after_sign_in" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".Calender.CalendarActivity" />
        <activity android:name=".AboutUsActivity" />
        <activity
            android:name=".Navigation.Navigation"
            android:label="@string/title_activity_navigation"
            android:theme="@style/NoActionBar"></activity>
    </application>

</manifest>